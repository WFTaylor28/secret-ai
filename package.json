{
  "name": "ai-character-chat",
  "version": "1.0.0",
  "description": "AI Character Chat App - Create and chat with custom AI characters",
  "homepage": "https://WFTaylor28.github.io/ai-character-chat ",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "eslintConfig": {
    "extends": ["react-app"]
  }
}
